# Representative Evidence Tree — fictional Todo Case 2

This is an illustrative schema walkthrough, not a measured agent result. The
JSON companion binds the frozen plan and preregistration digests, reports Core,
Observation, and Final separately, and retains the evidence for every selected
Skill. In a real run, fail-closed validation recomputes the score and verifies
the Evidence Tree digest before a result may be presented.

